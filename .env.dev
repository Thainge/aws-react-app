# Values in this file are used when Vite runs with `--mode dev`.
# Includes `npm run dev` and `npm run build:dev`.
# Vite only exposes variables prefixed with VITE_.

VITE_APP_TITLE=AWS React App (Dev)
VITE_API_URL=http://localhost:3001
VITE_AUTH_MODE=mock
VITE_MOCK_USER_EMAIL=admin@example.com
VITE_MOCK_USER_NAME=Admin User
VITE_MOCK_USER_PICTURE_URL=
VITE_MOCK_USER_ROLE=admin
VITE_COGNITO_USER_POOL_ID=
VITE_COGNITO_USER_POOL_CLIENT_ID=
